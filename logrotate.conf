# Trawler logrotate 配置
# 安装: sudo cp logrotate.conf /etc/logrotate.d/trawler
# 测试: sudo logrotate -d /etc/logrotate.d/trawler

/path/to/trawler/cron.log {
    daily
    rotate 7
    missingok
    notifempty
    compress
    delaycompress
    copytruncate
    dateext
}